# Example environment variables for Vite (prefix with VITE_ to be exposed to client)
VITE_API_BASE_URL=https://api.example.com
VITE_FEATURE_FLAG_EXPERIMENTAL=false
# Add secrets (without VITE_ prefix) only if they stay server-side; for pure SPA keep everything public-safe.
